import { FC } from "react";

interface BoardHighlightsProps {
  isPiece: boolean;
  isMainBoxColor: boolean;
}

export const ValidMoves: FC = ({ isPiece, isMainBoxColor }) => {
  return <div>
  </div>
}
